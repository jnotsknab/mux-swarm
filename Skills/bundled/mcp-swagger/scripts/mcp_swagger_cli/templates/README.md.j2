# {{ title }} - MCP Server

This is an MCP (Model Context Protocol) server generated from an OpenAPI specification. It provides access to the {{ title }} API through MCP tools and resources.

## Features

- **{{ operation_count }} API operations** mapped to MCP Tools
- **{{ path_count }} API endpoints**
- **Transport**: {{ transport }}
- **Base URL**: {{ base_url }}

## Installation

### Option 1: pip install (recommended)

```bash
cd {{ server_name }}
pip install -e .
```

### Option 2: Using uv (faster)

```bash
cd {{ server_name }}
uv pip install -e .
```

## Running the Server

### Stdio Transport (Default)

This is the recommended transport for local use with Claude Desktop:

```bash
# Using the installed command
{{ server_name }}

# Or directly with Python
python -m {{ server_name }}
```

### SSE/HTTP Transport

For remote access or testing:

```bash
# Start with SSE transport on port 8000
{{ server_name }} --sse 8000

# Or directly
python -m {{ server_name }} --sse 8000
```

## Configuration

### Claude Desktop

To use this server with Claude Desktop, add it to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "{{ server_name }}": {
      "command": "python",
      "args": ["-m", "{{ server_name }}"],
      "env": {
        "PYTHONPATH": "/path/to/{{ server_name }}"
      }
    }
  }
}
```

### Base URL

The server uses `{{ base_url }}` as the default base URL. To override:

```python
# In your code, modify main.py
BASE_URL = "https://your-api.example.com"
```

Or set an environment variable and modify the server to read it.

## Authentication

{% if api_key_env %}
This server reads your API key from the `{{ api_key_env }}` environment variable at runtime.

Set it before running:
```bash
export {{ api_key_env }}=your_api_key_here
```

The key is sent as: `{{ api_key_header }}: {{ api_key_prefix }} <your_key>`
{% else %}
No API key was configured at generation time. If the API requires authentication,
re-generate with `--api-key-env YOUR_ENV_VAR_NAME` or pass headers manually via `-H`.
{% endif %}

## Available Tools

{% if operations -%}
The following tools are available:

{% for operation in operations[:10] -%}
- `{{ operation.operation_id }}` - {{ operation.summary or operation.description or 'API operation' }}
{% endfor -%}
{% if operations | length > 10 -%}
- ... and {{ operations | length - 10 }} more
{% endif -%}

{% else -%}
No operations found in the specification.
{% endif -%}

## Available Resources

{% if schema_names -%}
- `schema://api/schemas` - List of all available schemas
{% for schema_name in schema_names[:10] -%}
- `schema://api/{{ schema_name | lower }}` - {{ schema_name }} schema
{% endfor -%}
{% if schema_names | length > 10 -%}
- ... and {{ schema_names | length - 10 }} more schemas
{% endif -%}
{% else -%}
No schemas found in the specification.
{% endif -%}

- `api://operations` - List of all available API operations

## Development

### Running Tests

```bash
pytest tests/ -v
```

### Building

```bash
# Using poetry
poetry build

# Or using the generated files
pip install -e .
pip install -r requirements.txt
```

## License

Generated by [mcp-swagger-cli](https://github.com/mcp-swagger/mcp-swagger-cli)

---

**Note**: This server was auto-generated. You may need to customize the implementation for production use, especially for:
- Authentication (API keys, OAuth, etc.)
- Error handling
- Rate limiting
- Request/response transformations
